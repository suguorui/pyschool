# You can use this code segment as template or rewrite everything on your own
def countX(s):
	"""Write a recursive function countX that takes in a string and returns the number of uppercase character 
	'X' in the String.
	"""
	if len(s) < 1:
		return 0
	else:
	        count = 0
	        if s[0] == 'X':
	                count = 1
	        return count + countX(s[1:])
