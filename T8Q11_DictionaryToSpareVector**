def convertDictionary(dictionary):
	if dictionary:   # avoid empty list
		M = max(dictionary.keys())
		vector = []
		for i in range(0,(M+1)):           #iterate through
			vector.append(dictionary.get(i,0))    #remember this .get() function
		return vector 
	else:
		return []
	
	
